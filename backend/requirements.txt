# Alt sınır (>=) kullanılıyor: fastmcp 3.x modern pydantic/httpx/starlette sürümleri
# istiyor, katı == pinleri çözülemez çakışmalara yol açıyordu. Kod yalnızca bu
# kütüphanelerin stabil API'lerini kullandığı için gevşetmek güvenli.
#
# Build başarılı olduktan sonra sürümleri kilitlemek için:
#   docker compose exec api pip freeze > backend/requirements.lock.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.11.7
pydantic-settings>=2.6.1
sqlalchemy>=2.0.35
alembic>=1.13.2
psycopg[binary]>=3.2.2
sse-starlette>=2.1.3
httpx>=0.27.2
python-dotenv>=1.0.1

# Kimlik doğrulama (app/core/security.py)
# bcrypt doğrudan kullanılıyor, passlib üzerinden değil: passlib'in bcrypt
# backend'i 4.x ile uyumsuz ve proje bakımsız; ihtiyacımız olan iki fonksiyon
# (hashpw/checkpw) zaten bcrypt'in kendi API'sinde.
bcrypt>=4.2.0
PyJWT>=2.9.0

langgraph>=0.2.28
fastmcp>=3.4.6
# ==0.5.5 kasıtlı sabit: docker-compose.yml'deki chroma server image'ı da
# chromadb/chroma:0.5.5. Client >=1.0 yeni bir HTTP API (v2 auth/identity)
# konuşuyor ve bu sürümdeki server'da 404 alıyor — server'ı yükseltmeden
# client'ı gevşetmeyin.
chromadb==0.5.5

# RAG (rag/ingest.py, rag/vector_store.py) — saf getirme, LLM yok
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0

Faker>=26.0.0

# Piyasa verisi sağlayıcıları (backend/app/providers/)
requests>=2.32.3
yfinance>=0.2.43
pandas>=2.2.2
tefas-crawler>=0.3.4
lxml>=5.3.0
# KAP canlı bildirim sorgusu (app/providers/kap_p.py). Resmi degil, MIT
# lisansli, PyPI'de aktif bakimli (bkz. kap_p.py docstring'i).
pykap>=0.2.0

pytest>=8.3.3
pytest-asyncio>=0.24.0

ruff>=0.6.9
black>=24.8.0
PyYAML>=6.0.1