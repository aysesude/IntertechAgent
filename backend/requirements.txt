# Alt sınır (>=) kullanılıyor: fastmcp 3.x modern pydantic/httpx/starlette sürümleri
# istiyor, katı == pinleri çözülemez çakışmalara yol açıyordu. Kod yalnızca bu
# kütüphanelerin stabil API'lerini kullandığı için gevşetmek güvenli.
#
# Build başarılı olduktan sonra sürümleri kilitlemek için:
#   docker compose exec api pip freeze > backend/requirements.lock.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.11.7
pydantic-settings>=2.6.1
sqlalchemy>=2.0.35
alembic>=1.13.2
psycopg[binary]>=3.2.2
sse-starlette>=2.1.3
httpx>=0.27.2
python-dotenv>=1.0.1

langgraph>=0.2.28
fastmcp>=3.4.6
chromadb>=0.5.5

Faker>=26.0.0

pytest>=8.3.3
pytest-asyncio>=0.24.0

ruff>=0.6.9
black>=24.8.0
